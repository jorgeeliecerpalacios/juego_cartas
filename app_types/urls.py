# urls app_types

from django.urls import path

# Views

from app_types import views

urlpatterns = [
    path(
        # route='', 
        # view=views.list_posts, 
        # name='feed'
        ),
]